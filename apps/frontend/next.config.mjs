/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'foodish-api.com', 'images.unsplash.com', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;
